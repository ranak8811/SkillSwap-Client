import React from "react";

const UserManagement = () => {
  return (
    <div>
      UserManagement
      {/* I will add table to handle users and other designs also buttons and graph if needed later. */}
    </div>
  );
};

export default UserManagement;
